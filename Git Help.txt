git status		# Check status

git config --global user.name "Put in user name"
git config --global user.email "Put in email"

git pull origin master		# Puts files on local folder

git commit -a		# Uploads local file online

git add *.(filetype ex. txt or py)

git commit -a then do i for insert then comment what you want. then press esc then write :wq for write quit.

git push origin master for finally uploading the files